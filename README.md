How to Compile and Run:

-To build Quash use:

bash$ make (all)

-To generate this documentation in HTML and LaTeX use:

bash$ make doc

-To clean quash use:

bash$ make clean

-To run Quash use:

./quash
